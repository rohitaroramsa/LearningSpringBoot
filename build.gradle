apply from: 'gradle/gatling.gradle'

// NOTE: I prepended the apply-from line to enable gradle/gatling.gradle. If your project already contains a build.gradle with important configuration, please merge this line into it instead of replacing existing content.